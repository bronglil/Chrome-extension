{
  "name": "snapshot-studio",
  "version": "1.0.0",
  "private": true,
  "description": "SnapShot Studio — MV3 screenshot, recording & annotation Chrome extension.",
  "scripts": {
    "test": "node --test \"test/**/*.test.js\"",
    "test:e2e": "xvfb-run -a playwright test -c e2e/playwright.config.js",
    "test:all": "npm test && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "qrcode": "^1.5.4"
  }
}
